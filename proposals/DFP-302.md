# DFP 302: 标准库错误处理机制

**作者**: 771835 <2790834181@qq.com>  
**状态**: Draft  
**创建日期**: 2025-04-05  
**最新更新**: 2026-04-05

---

## 摘要

本提案定义 Dovetail 标准库的错误处理模式和最佳实践，在无异常机制的语言约束下，提供统一的错误检测和报告方案。

---

## 动机

当前问题：

1. 函数遇到非法输入返回魔数（-1, 0），语义不明确
2. 无法区分"正常返回 0"和"错误返回 0"
3. 错误信息无法传播到调用者
4. 调试困难，无运行时检查

示例：

```mcdl
// 当前实现：错误难以检测
let result = isqrt(-5)  // 返回 0，但 0 也是合法值
if (result == 0) {
    // 无法判断是输入为 0 还是输入非法
}
```

---

## 技术规范

### 1. 错误处理模式

由于 Dovetail 暂无异常机制，采用以下模式：

#### 模式 1：返回值 + 全局错误码

```mcdl
// errorlib.mcdl - 错误处理核心库

let ERRNO_OK = 0                    // 无错误
let ERRNO_INVALID_ARG = 1           // 非法参数
let ERRNO_OUT_OF_RANGE = 2          // 超出范围
let ERRNO_OVERFLOW = 3              // 溢出
let ERRNO_UNDERFLOW = 4             // 下溢
let ERRNO_DIV_BY_ZERO = 5           // 除零错误
let ERRNO_NOT_FOUND = 6             // 未找到
let ERRNO_ALREADY_EXISTS = 7        // 已存在

// 全局错误码（类似 C 的 errno）
let g_errno = 0

/**
 * 设置全局错误码
 */
fn set_error(error_code: int) {
    g_errno = error_code
}

/**
 * 获取并清除错误码
 */
fn get_and_clear_error() -> int {
    let err = g_errno
    g_errno = ERRNO_OK
    return err
}

/**
 * 检查是否有错误
 */
fn has_error() -> boolean {
    return g_errno != ERRNO_OK
}
```

#### 模式 2：Option 类型模拟（使用哨兵值）

```mcdl
// 对于可能失败的函数，定义明确的失败值
let INVALID_INDEX = -1
let INVALID_COORD = 2147483647      // INT_MAX 作为哨兵

fn find_element(arr: array<int>, target: int) -> int {
    // 返回索引，未找到时返回 INVALID_INDEX
    for (let i = 0; i < array_length(arr); i += 1) {
        if (arr[i] == target) {
            return i
        }
    }
    return INVALID_INDEX
}

// 使用示例
let index = find_element(my_array, 42)
if (index != INVALID_INDEX) {
    // 找到了
} else {
    // 未找到
}
```

#### 模式 3：布尔返回 + 引用参数

```mcdl
/**
 * 安全的整数平方根（带错误检查）
 * 
 * 参数:
 *   x - 输入值
 *   out_result - 输出结果（通过引用传递）
 * 
 * 返回值:
 *   true 表示成功，false 表示失败
 * 
 * 注：需要编译器支持引用传递
 */
fn safe_isqrt(x: int, out_result: &int) -> boolean {
    if (x < 0) {
        set_error(ERRNO_INVALID_ARG)
        return false
    }
    
    out_result = isqrt(x)
    return true
}

// 使用示例
let result = 0
if (safe_isqrt(value, &result)) {
    // 成功，使用 result
} else {
    // 失败，检查 g_errno
}
```

---

### 2. 标准库函数改进示例

#### 改进前（原 mathlib.mcdl）

```mcdl
fn pow(base: int, exp: int) -> int {
    if (exp < 0) {
        return 0  // 魔数！
    }
    // ...
}
```

#### 改进后（带错误处理）

```mcdl
include "errorlib"

/**
 * 快速幂（带错误检查）
 * 
 * 参数:
 *   base - 底数
 *   exp - 指数（必须非负）
 * 
 * 返回值:
 *   计算结果，失败时返回 0 并设置 g_errno
 * 
 * 错误码:
 *   ERRNO_INVALID_ARG - exp < 0
 *   ERRNO_OVERFLOW - 结果溢出（可选检测）
 */
fn pow(base: int, exp: int) -> int {
    if (exp < 0) {
        set_error(ERRNO_INVALID_ARG)
        return 0
    }
    
    if (exp == 0) {
        return 1
    }

    // 溢出检测（可选）
    if (abs(base) > 1 && exp > 30) {
        set_error(ERRNO_OVERFLOW)
        return 0
    }

    let result = 1
    let currentBase = base

    while (exp > 0) {
        if (exp % 2 == 1) {
            result = result * currentBase
        }
        currentBase = currentBase * currentBase
        exp = exp / 2
    }

    return result
}

// 使用示例
let result = pow(2, 10)
if (has_error()) {
    let err = get_and_clear_error()
    if (err == ERRNO_INVALID_ARG) {
        print("错误：指数不能为负数")
    }
} else {
    print(f"结果：{result}")
}
```

---

### 3. 断言和调试工具

```mcdl
// debuglib.mcdl - 调试工具库

/**
 * 断言宏（需编译器支持宏或编译时优化）
 * 
 * 用法：
 *   assert(x > 0, "x must be positive")
 * 
 * 生产环境下可通过编译选项禁用
 */
fn assert(condition: boolean, message: string) {
    if (!condition) {
        print(f"[ASSERT FAILED] {message}")
        exec("tellraw @a {\"text\":\"Assertion failed\",\"color\":\"red\"}")
        // 可选：强制停止或记录日志
    }
}

/**
 * 运行时边界检查
 */
fn check_bounds(value: int, min: int, max: int, var_name: string) -> boolean {
    if (value < min || value > max) {
        print(f"[BOUNDS ERROR] {var_name} = {value}, expected [{min}, {max}]")
        set_error(ERRNO_OUT_OF_RANGE)
        return false
    }
    return true
}

/**
 * 调试输出（带时间戳）
 */
fn debug_log(message: string) {
    // 假设有获取游戏时间的方法
    // let time = game_time()
    print(f"[DEBUG] {message}")
}

/**
 * 性能计时器
 */
let g_timer_start = 0

fn timer_start() {
    // g_timer_start = game_time()
}

fn timer_stop(label: string) {
    // let elapsed = game_time() - g_timer_start
    // print(f"[TIMER] {label}: {elapsed} ticks")
}
```

---

### 4. 使用示例

#### 示例 1：数组访问安全封装

```mcdl
include "errorlib"
include "debuglib"

fn array_get_safe(arr: array<int>, index: int) -> int {
    if (!check_bounds(index, 0, array_length(arr) - 1, "index")) {
        return 0  // 返回默认值
    }
    return arr[index]
}

// 使用
let value = array_get_safe(my_array, 10)
if (has_error()) {
    print("数组访问越界！")
}
```

#### 示例 2：链式错误传播

```mcdl
fn complex_calculation(x: int) -> int {
    let step1 = pow(x, 2)
    if (has_error()) return 0
    
    let step2 = isqrt(step1)
    if (has_error()) return 0
    
    return step2
}
```

---

## 最佳实践

### 1. 函数设计原则

- 所有可能失败的函数应设置错误码
- 文档明确列出所有可能的错误码
- 使用明确的哨兵值（避免魔数）
- 提供"安全版本"和"快速版本"两种 API

### 2. 调用者责任

- 调用可能失败的函数后检查错误
- 及时清除错误码（避免误判）
- 在调试模式启用断言

### 3. 错误恢复策略

- 返回默认值（如 0、空字符串）
- 跳过当前操作继续执行
- 记录日志并通知管理员

---

## 未来扩展

### 编译器支持

- 编译时常量检查
- 未使用返回值警告
- 自动插入边界检查代码

## 变更日志

2026-04-05：初始提案创建