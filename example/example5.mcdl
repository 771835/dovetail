func int main() {
    /*
    boolean cond_var;
    if (cond_var) {
        # if块代码
    } else {
        # else块代码
    }

    int m =1;
    cmd(f"sawa{m}");
    */
    for (int i=0;i<3;i=i+1) {
        # 循环体
        cmd(f"say for_{i}");
    }
/*

    func add(a:int, b:int) -> int {
        return a + b
    }

    int r = add(1, 2);
    cmd(f"say ${r}")
    */
}