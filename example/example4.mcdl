include "random"
include "mathlib.mcdl"
@init
func main() fuck int{

    for (int i =0;i<=300;i=i+1) {
        let f = abs(fibonacci(i))
        exec(f"say {f}")
    }

    /*

    func int a(){
        exec("say 1")
        _call("a")
    }
    a()
*/
}