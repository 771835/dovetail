# 过时的示例，请不要使用他
include "int_list"
include "random"

@init
func main() {
    let a = IntList();
    # 此处必须手动清理
    a.clear()
    a.append(1)
    a.append(2)
    a.append(3)
    a.append(1)
    a.pop()
    a.append(4)
    for (int i =0;i<4;i=i+1) {
        print(f"i={i}")
        print(str(a[i]))
    }


}